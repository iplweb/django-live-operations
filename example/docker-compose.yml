services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  web:
    build:
      context: ..
      dockerfile: example/Dockerfile
    command: web
    ports:
      - "8000:8000"
    environment:
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/0
      RUNNER: celery
      SECRET_KEY: demo-docker-key-change-in-production
      DEBUG: "true"
      # Shared SQLite file so web + worker see the same database.
      SQLITE_PATH: /data/db.sqlite3
    volumes:
      - static_root:/app/staticroot
      - db_data:/data
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      # /healthz/ is side-effect-free (plain 200); /__login__/ would create a
      # session row on every poll. start_interval polls fast during startup so
      # the worker (depends_on web healthy) starts quickly, then interval backs
      # off to keep steady-state logs quiet.
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/healthz/"]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 30s
      start_interval: 2s

  worker:
    build:
      context: ..
      dockerfile: example/Dockerfile
    command: worker
    environment:
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/0
      RUNNER: celery
      SECRET_KEY: demo-docker-key-change-in-production
      # Same shared SQLite file as web (see settings.SQLITE_PATH).
      SQLITE_PATH: /data/db.sqlite3
    volumes:
      - db_data:/data
    depends_on:
      redis:
        condition: service_healthy
      # Start only after web has migrated + seeded the shared DB, so the
      # worker never races web on first-boot migrations.
      web:
        condition: service_healthy

volumes:
  static_root:
  db_data:
